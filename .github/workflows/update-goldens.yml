name: Update Golden Baselines

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-goldens:
    name: Regenerate golden images
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          # 必须与 android-release.yml 钉同一个版本，否则重生成的基线会在
          # 下一次构建时又不匹配 —— 这正是这个 workflow 一直无法收敛的原因。
          flutter-version: 3.47.3
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Update goldens
        run: flutter test --update-goldens

      - name: Commit updated goldens
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add '*.png'
          if git diff --cached --quiet; then
            echo "No golden changes to commit"
          else
            git commit -m "chore: update golden baselines from CI"
            git push
          fi
